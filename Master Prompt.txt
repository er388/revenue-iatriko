# Revenue Management System - Full Stack Development

## 📋 Ρόλος & Context

Είσαι ένας έμπειρος full-stack JavaScript developer με εξειδίκευση σε:
- Vanilla JS (ES6+) χωρίς frameworks
- Modular architecture με ES6 modules
- IndexedDB & localStorage για client-side storage
- Canvas-based visualizations (Chart.js)
- CSS3 με custom properties για theming
- Progressive Web App (PWA) principles

## 🎯 Αποστολή

Θα δημιουργήσεις από την αρχή ένα **Revenue Management System** για ιατρούς/διαγνωστικά κέντρα που διαχειρίζεται:
- Έσοδα από ΕΟΠΥΥ με 5 τύπους κρατήσεων (Παρακράτηση, ΜΔΕ, Rebate, Κρατήσεις, Clawback)
- Έσοδα από άλλα ταμεία με 1 τύπο κράτησης
- Οπτικοποίηση με charts, heatmaps, forecasting
- Backup/restore, CSV import/export, PDF exports
- Cloud sync (Google Drive, Dropbox, OneDrive)

Έχω στη διάθεσή σου **όλα τα υπάρχοντα αρχεία** ως reference, αλλά θέλω **clean rewrite** με:
1. **Modular structure** - κάθε module να κάνει ΕΝΑ πράγμα καλά
2. **AI-friendly code** - σαφή ονόματα, καλά comments, predictable patterns
3. **Incremental development** - να χτίζεται σε φάσεις με testing σε κάθε βήμα

---

## 🏗️ Αρχιτεκτονική Σύστημα

### Core Modules (Priority 1 - MVP)
```
1. state.js          → Global state management
2. storage.js        → IndexedDB + localStorage wrapper
3. utils.js          → Helper functions (dates, currency, validation)
4. dataManager.js    → CRUD operations for entries
5. eopyyClawback.js  → Deductions calculation engine
6. uiRenderers.js    → All UI rendering functions
7. formHandlers.js   → Form validation & interactions
8. eventHandlers.js  → Event listeners & callbacks
9. app.js            → Main orchestrator
```

### Extended Modules (Priority 2)
```
10. filters.js       → Advanced filtering logic
11. charts.js        → Heatmaps & advanced charts
12. comparison.js    → Period comparison
13. forecasting.js   → Revenue forecasting
14. backup.js        → Backup/restore + autosave
15. pdfExport.js     → PDF generation
16. csvValidator.js  → CSV import validation
17. cloudAdapters.js → Cloud storage integrations
18. cdnChecker.js    → CDN availability monitoring
```

---

## 📦 Data Structure

### Entry Object
```javascript
{
  id: string,              // Unique ID
  date: string,            // "MM/YYYY"
  source: string,          // Diagnostic center name
  insurance: string,       // "ΕΟΠΥΥ" or other
  type: string,            // "cash" | "invoice"
  amount: number,          // Original amount
  originalAmount: number,  // Same as amount (for clarity)
  
  // ΕΟΠΥΥ deductions (only if insurance.includes('ΕΟΠΥΥ'))
  deductions: {
    parakratisi: number,
    parakratisiPercent: number,
    mde: number,
    mdePercent: number,
    rebate: number,
    rebatePercent: number,
    krathseis: number,
    krathseisPercent: number,
    clawback: number,
    clawbackPercent: number
  },
  
  // Non-ΕΟΠΥΥ deductions
  krathseis: number,
  krathseisPercent: number,
  
  notes: string,
  createdAt: number,
  updatedAt: number,
  createdBy: string,
  updatedBy: string
}
```

### Calculation Rules
```javascript
// ΕΟΠΥΥ Entries
totalDeductions = parakratisi + mde + rebate + krathseis + clawback
finalAmount = originalAmount - totalDeductions
finalAmountNoParakratisi = originalAmount - (mde + rebate + krathseis + clawback)

// Non-ΕΟΠΥΥ Entries
finalAmount = originalAmount - krathseis
```

---

## 🎨 UI Structure

### Views (Tabs)
```html
1. Dashboard     → KPIs, Quick Add, Recent Entries, Charts
2. Entries       → Full table with filters, pagination, CRUD
3. Reports       → Annual/period reports (TO IMPLEMENT)
4. Comparison    → Side-by-side period comparison (TO IMPLEMENT)
5. Forecasting   → Revenue predictions (TO IMPLEMENT)
6. Heatmaps      → Visual heatmaps (TO IMPLEMENT)
7. Cloud         → Cloud storage sync (TO IMPLEMENT)
8. Settings      → Sources, insurances, backup, autosave
```

### Dashboard Components
```
- Quick Add Form (compact, with deduction fields)
- Period Selector (All/Month/Year)
- Toggle "Με Παρακράτηση" (default: OFF)
- 9 KPI Cards (Total, ΕΟΠΥΥ, Others, + 5 deduction breakdowns)
- 2 Charts (Pie chart, Monthly trend)
- Recent Entries Table (collapsible)
```

---

## 🚀 Development Phases

### Phase 1: Core Foundation (MVP)
**Deliverables:**
- [ ] State management + storage setup
- [ ] Basic entry CRUD (add, edit, delete)
- [ ] Simple dashboard with KPIs
- [ ] ΕΟΠΥΥ deductions logic
- [ ] Basic entries table

**Testing Checklist:**
```
✓ Add ΕΟΠΥΥ entry with all 5 deductions
✓ Add non-ΕΟΠΥΥ entry with 1 deduction
✓ Edit entry and verify deductions persist
✓ Delete entry
✓ Dashboard KPIs update correctly
✓ "Με Παρακράτηση" toggle works
```

### Phase 2: UI Enhancement
**Deliverables:**
- [ ] Filters (date range, source, insurance, amounts, deductions %)
- [ ] Pagination with configurable page size
- [ ] Sortable table columns
- [ ] Charts (Chart.js integration)
- [ ] Form auto-calculations (amount ↔ percent sync)

**Testing Checklist:**
```
✓ Filters work correctly
✓ Pagination works
✓ Column sorting (ASC/DESC)
✓ Charts render properly
✓ Entering amount auto-calculates %
✓ Entering % auto-calculates amount
```

### Phase 3: Advanced Features
**Deliverables:**
- [ ] CSV import/export
- [ ] PDF exports
- [ ] Backup/restore with merge/overwrite
- [ ] Autosave (configurable interval)
- [ ] Undo/redo functionality

### Phase 4: Analytics & Cloud
**Deliverables:**
- [ ] Reports view
- [ ] Comparison view
- [ ] Forecasting view
- [ ] Heatmaps view
- [ ] Cloud storage integrations

---

## 🎯 Immediate Requirements (από Prompt.txt)

### 1️⃣ Προεπιλεγμένη Λογική Παρακράτησης
```
ΑΛΛΑΓΗ: Όλοι οι υπολογισμοί "Τελικού Ποσού" πρέπει 
ΑΠΟ ΠΡΟΕΠΙΛΟΓΗ να ΑΦΑΙΡΟΥΝ την παρακράτηση.

Η επιλογή "Με Παρακράτηση" την ΠΡΟΣΘΕΤΕΙ ξανά.

Τρέχουσα λογική (ΛΑΘΟΣ):
- Default: WITH parakratisi
- Checkbox: removes it

Νέα λογική (ΣΩΣΤΗ):
- Default: WITHOUT parakratisi  
- Checkbox: adds it back
```

### 2️⃣ Dashboard Layout Changes
```
ΜΕΤΑΚΙΝΗΣΗ: Το header "Επισκόπηση - Όλα - Με Παρακράτηση"
να πάει ΚΑΤΩ από τη "Γρήγορη Καταχώριση"
και ΠΑΝΩ από τα KPIs/Charts.

Νέα σειρά:
1. Γρήγορη Καταχώριση
2. Επισκόπηση - Φίλτρα (← εδώ)
3. KPI Cards
4. Charts
5. Πρόσφατες Εγγραφές
```

### 3️⃣ KPI Cards Redesign
```
Νέο layout ανά card:
┌─────────────────────┐
│   Τίτλος (κέντρο)   │
├──────────┬──────────┤
│ Ποσό     │  xx,xx % │
│ (αριστερά)│ (δεξιά) │
└──────────┴──────────┘

Ποσοστό = (KPI / Total) * 100
```

### 4️⃣ Entries Table Improvements
```
1. Πρόσθεσε στήλες για κάθε κράτηση:
   - Παρακράτηση
   - ΜΔΕ
   - Rebate
   - Κρατήσεις
   - Clawback

2. Στοίχιση στηλών (fix alignment)

3. Σελιδοποίηση: 25, 50, 75, 100, 150 εγγραφές/σελίδα

4. Sortable columns (click header → ASC/DESC)
```

### 5️⃣ Form Enhancements
```
1. Αποθήκευση ποσοστών μαζί με τα ποσά
2. Clawback period selector (monthly/quarterly/semi-annual/annual)
3. Auto-calculation: amount ↔ percent sync
4. Bug fix: Ποσοστά δεν "κολλάνε" σε επόμενο modal
5. Bug fix: "Τελικό Ποσό" να μηδενίζει μετά από submit
```

### 6️⃣ Filter Improvements
```
Φίλτρο "Ποσό Έως": 
- Από 99999 → 9999999
```

### 7️⃣ Modal Behavior
```
Όλα τα modals:
- Draggable (μετακινήσιμα)
- Resizable (αλλαγή μεγέθους)
```

### 8️⃣ Autosave Configuration
```
Ρυθμιζόμενο όριο: 1, 3, 5, 10, 20 αλλαγές
Default: 5 αλλαγές
```

---

## 📝 Coding Standards

### Naming Conventions
```javascript
// Variables: camelCase
let totalAmount = 0;
let isEopyyEntry = false;

// Constants: UPPER_SNAKE_CASE
const MAX_ENTRIES = 1000;
const DEFAULT_PAGE_SIZE = 20;

// Functions: camelCase + verb prefix
function calculateFinalAmount() {}
function renderEntriesTable() {}
function validateEntry() {}

// Classes: PascalCase
class EopyyDeductionsManager {}
class BackupManager {}

// Private methods: _prefix
_calculatePercentage() {}
```

### Module Pattern
```javascript
// state.js
export const STATE = { ... };
export function updateState() {}
export default { STATE, updateState };

// dataManager.js
export async function loadData() {}
export async function saveData() {}
export default { loadData, saveData };

// Usage in app.js
import { STATE } from './state.js';
import { loadData } from './dataManager.js';
```

### Error Handling
```javascript
// Always wrap in try-catch
async function saveEntry(entry) {
  try {
    await storage.saveEntries([entry]);
    showToast('Επιτυχής αποθήκευση', 'success');
    return true;
  } catch (error) {
    console.error('Save entry error:', error);
    showToast('Σφάλμα αποθήκευσης', 'error');
    return false;
  }
}
```

### Comments
```javascript
// ✅ GOOD: Explains WHY
// Calculate without parakratisi because it's already collected
const finalAmount = originalAmount - (mde + rebate + krathseis + clawback);

// ❌ BAD: Explains WHAT (obvious)
// Subtract deductions from amount
const finalAmount = originalAmount - totalDeductions;
```

---

## 🔄 Development Workflow

### For Each Phase:
```
1. Σου δίνω το scope του module/feature
2. Μου δίνεις:
   a) Σχέδιο υλοποίησης (bullet points)
   b) Dependencies (ποια modules χρειάζεται)
   c) Testing checklist
3. Περιμένεις το ΟΚ μου
4. Μου δίνεις τον κώδικα σε chunks:
   - Module A (πλήρης κώδικας)
   - Που να μπει (path)
   - Τι να ελέγξω (specific tests)
5. Σου δίνω feedback
6. Προχωράμε στο επόμενο chunk

**ΣΗΜΑΝΤΙΚΟ:** 
- ΕΝΑ module τη φορά
- Περιμένεις επιβεβαίωση πριν συνεχίσεις
- Αν κάτι δεν δουλεύει, το φτιάχνουμε πριν προχωρήσουμε
```

---

## 🎬 Ξεκίνα με Phase 1

**Πρώτο Βήμα:**
Δώσε μου το implementation plan για τα **3 βασικά modules**:
1. `state.js` - Global state
2. `storage.js` - IndexedDB wrapper
3. `utils.js` - Helper functions

Για το καθένα, περιγράψε:
- Τι export functions/constants θα έχει
- Ποιες είναι οι κύριες λειτουργίες του
- Dependencies (αν υπάρχουν)

Περιμένω το σχέδιό σου πριν γράψεις κώδικα.

---

## 📚 Reference Files

Έχεις πρόσβαση σε όλα τα υπάρχοντα αρχεία για reference:
- Δομή δεδομένων
- Calculation logic
- UI patterns
- Existing implementations

**ΑΛΛΑ:** Θέλω clean rewrite, όχι copy-paste. Χρησιμοποίησέ τα ως inspiration.

---

## ✅ Success Criteria

Το project είναι επιτυχημένο όταν:
- [ ] Όλα τα modules είναι independent & testable
- [ ] Ο κώδικας είναι AI-friendly (σαφής, predictable)
- [ ] Κάθε feature λειτουργεί σωστά πριν προχωρήσουμε
- [ ] Έχει πλήρες documentation (inline comments)
- [ ] Δεν υπάρχουν hardcoded values (όλα σε CONFIG/STATE)
- [ ] Responsive design (mobile-friendly)
- [ ] Offline-capable (PWA ready)

---

## 🚦 Ready to Start?

Απάντησε με το implementation plan για Phase 1, Step 1:
**state.js + storage.js + utils.js**

Περιμένω! 🎯